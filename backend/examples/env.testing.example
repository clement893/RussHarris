# Testing Environment Configuration
# Use this for running tests

# ============================================
# PROJECT CONFIGURATION
# ============================================
PROJECT_NAME=Test App
ENVIRONMENT=testing
DEBUG=True

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Use in-memory SQLite or test PostgreSQL database
# Tests use in-memory SQLite by default, but can use PostgreSQL
DATABASE_URL=sqlite+aiosqlite:///:memory:
# Or: DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/test_db

# ============================================
# SECURITY CONFIGURATION
# ============================================
# Test secret key (not for production!)
SECRET_KEY=test-secret-key-for-testing-only-not-for-production

# JWT Configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=90

# ============================================
# CORS CONFIGURATION
# ============================================
CORS_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# ============================================
# FEATURE FLAGS (Testing)
# ============================================
# Disable features that might interfere with testing
DISABLE_CSRF=true
DISABLE_RATE_LIMITING=true
ENABLE_REQUEST_SIGNING=false

# ============================================
# OPTIONAL SERVICES (Mocked in tests)
# ============================================
# These can be empty - tests will mock these services
STRIPE_SECRET_KEY=
SENDGRID_API_KEY=
REDIS_URL=
AWS_ACCESS_KEY_ID=
OPENAI_API_KEY=

# ============================================
# NOTES
# ============================================
# - Tests use in-memory database by default
# - External services are mocked in tests
# - No real API calls are made during testing
# - Use this config when running pytest

