# Minimal Configuration Example
# Use this if you want to run the backend with minimal dependencies
# Only requires: PostgreSQL database

# ============================================
# REQUIRED CONFIGURATION
# ============================================

# Project Name
PROJECT_NAME=My App

# Environment
ENVIRONMENT=development

# Database (REQUIRED)
# PostgreSQL connection string
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/mydb

# Security (REQUIRED)
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-min-32-characters-long

# CORS (REQUIRED)
# Frontend URL
FRONTEND_URL=http://localhost:3000

# ============================================
# OPTIONAL - DISABLE FEATURES
# ============================================

# Disable rate limiting (no Redis needed)
DISABLE_RATE_LIMITING=true

# Disable CSRF (easier for development)
DISABLE_CSRF=true

# ============================================
# NOTES
# ============================================
# This minimal config allows you to run the backend with:
# - PostgreSQL database (required)
# - No Redis (caching disabled)
# - No Stripe (payments disabled)
# - No SendGrid (email disabled)
# - No S3 (file storage disabled)
# - No OAuth (OAuth disabled)
# - No AI providers (AI features disabled)

# All optional features will gracefully degrade if not configured.
# The backend will start and run, but those features won't be available.

